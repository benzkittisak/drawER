# Normalize line endings to LF in the repo; check out native on each OS.
* text=auto eol=lf

# Golden SQL fixtures stay LF so the comparison is stable across OSes
*.sql text eol=lf

# Binary assets
*.png binary
*.jpg binary
*.woff2 binary
